Average Complexity O(nlogn)
Worst Complexity O(n²)

It works by selecting a 'pivot' element
from the array and partitioning the other 
elements into two sub-arrays, according to 
whether they are less than or greater than the pivot.

Purple rectangle is the pivot used for comparison
Blue rectangles are the ones that are compared to pivot